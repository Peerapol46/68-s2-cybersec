   services:
    s2admin:
      image: dpage/pgadmin4:latest
      container_name: 68-s2-admin
      ports:
        - "${ADMIN_PORT}:80"
      environment:
        PGADMIN_DEFAULT_EMAIL: ${ADMIN_DEFAULT_USER}
        PGADMIN_DEFAULT_PASSWORD: ${ADMIN_DEFAULT_PASS}